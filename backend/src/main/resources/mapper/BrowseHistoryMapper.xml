<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.BrowseHistoryMapper">
    <insert id="insertOrUpdate">
        INSERT INTO browse_history (user_id, product_id, category_id, viewed_at)
        VALUES (#{userId}, #{productId}, #{categoryId}, NOW())
        ON DUPLICATE KEY UPDATE category_id = #{categoryId}, viewed_at = NOW()
    </insert>

    <select id="selectByUserAndCategory" resultType="com.shop.entity.BrowseHistory">
        SELECT id, user_id, product_id, category_id, viewed_at
        FROM browse_history
        WHERE user_id = #{userId} AND category_id = #{categoryId}
        ORDER BY viewed_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectRecentProductIdsByUser" resultType="java.lang.Long">
        SELECT product_id
        FROM browse_history
        WHERE user_id = #{userId}
        ORDER BY viewed_at DESC
        LIMIT #{limit}
    </select>
</mapper>
